{
  "version": 2,
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm install && npm run build; else echo 'Frontend folder not found'; exit 1; fi",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install && if [ -d backend ]; then cd backend && npm install && cd ..; fi && if [ -d frontend ]; then cd frontend && npm install && cd ..; fi",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

